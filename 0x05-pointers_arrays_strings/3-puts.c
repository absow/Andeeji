#include "main.h"
/**
 * _puts - prints a strings
 * @str: string
 * Return: void
 */

void _puts(char *str)
{
	int i = 0;

	while (i[str] != '\0')
	{
		_putchar(i[str]);
		i++;
	}

	_putchar('\n');
}
